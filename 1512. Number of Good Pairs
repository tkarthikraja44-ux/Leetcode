class Solution {
    public int numIdenticalPairs(int[] nums) {
        Map<Integer,Integer> hm = new HashMap<>();
        int gp=0;
        for(int ele:nums)
        {
            gp=gp+hm.getOrDefault(ele,0);
            hm.put(ele,hm.getOrDefault(ele,0)+1);
        } 
        return gp;
    }
}
