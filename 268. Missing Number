class Solution {
    public int missingNumber(int[] nums) {
        int size = nums.length;
        int sum=0;
        for(int i=1;i<=size;i++)
        {
            sum+=i;
        }
        int t=sum;
        for(int i=0;i<size;i++)
        {
            t-=nums[i];
        }
        return t;

    }
}
